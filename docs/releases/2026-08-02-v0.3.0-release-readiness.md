# v0.3.0 Release Readiness Report

**Audit date:** 2026-08-02  
**Release scope:** Evidence-first multi-provider research milestone  
**Decision:** READY FOR REVIEW AND COMMIT

This report records repository state before the release commit. It does not
assert that a Git tag, GitHub release, or production deployment already
exists.

## Technical Readiness — PASS

- 70 unit tests pass;
- Python compilation passes for the runner, diagnostics, DP Document provider
  modules, and research-summary tooling;
- `providers.json` validates as 11 provider entries with nine discovery
  profiles and two landing-only profiles;
- every configured provider entrypoint exists;
- relative Markdown links and internal anchors resolve;
- `git diff --check` passes;
- the working tree contains the intentional release changes and no tracked
  runtime artifact.

## Governance Readiness — PASS

- the Evidence Matrix and `providers.json` agree on nine approved discovery
  profiles: Madrid, Barcelona, London, Milan, Valencia, Berlin, Toronto,
  Cologne, and Bratislava;
- Kortrijk and Chisinau remain landing-only;
- the runtime discovery allowlist contains exactly the nine governed profile
  identifiers;
- Berlin, Cologne, Bratislava, and Toronto were admitted by the explicit
  2026-08-02 governance review and completed bounded validation;
- the Kortrijk candidate probe did not detect identifiers and did not promote
  capability;
- runtime `BLOCKED`, `UNKNOWN`, and temporary `NO_SLOTS` outcomes did not edit
  trusted configuration.

## Traceability Readiness — PASS

The enabled capability chain is complete:

```text
Reviewed live research and cross-deployment comparison
  -> Evidence Matrix
  -> ADR-0010 or 2026-08-02 governance review
  -> providers.json
  -> CityMonitor runtime guard
```

The original five profiles are governed by ADR-0010 and their retained
research notes. Berlin, Cologne, Bratislava, and Toronto reference the
2026-08-02 governance decision and deployment-specific live notes. The
six-hour validation provides bounded operational evidence without replacing
those governance decisions.

## Operational Validation — PASS

Run `RUN-20260801-234419-release-validation` completed automatically after
21,600 seconds and stopped all supervised processes cleanly.

- 241 HTTP-first cycles;
- 213 blocked HTTP landings;
- 202 confirmed Playwright discovery executions;
- 117 executions reached `TIMES`;
- 85 executions stopped at an earlier recognized `NO_SLOTS` boundary;
- zero browser errors and zero browser `UNKNOWN`;
- zero CAPTCHA, identity-data, or booking interactions;
- one Kortrijk candidate probe stopped at `LANDING` without identifiers.

Cologne remained `NO_SLOTS` throughout this window. This does not revoke its
earlier live-confirmed contract. Kortrijk yielded no promotion evidence and
remains landing-only.

## Repository Hygiene — PASS

- `.browser-data/`, logs, metadata, observations, research output, runtime
  databases, Python caches, environment files, HAR, trace, and session files
  are Git-ignored;
- no tracked path matches the audited runtime-artifact patterns;
- the repository secret-pattern scan found no committed cookie header,
  authorization value, bearer value, serialized CSRF value, persistent
  fingerprint value, or phone-number payload;
- generated full research summaries, browser state, screenshots, cookies,
  CSRF values, local/session storage, and candidate runtime artifacts remain
  local.

## Documentation Consistency — PASS

- README, Architecture, Decisions, Providers, Roadmap, Changelog, Evidence
  Matrix, Release Policy, research index, plans, specifications, and localized
  summaries reflect nine governed profiles and two landing-only deployments;
- Playwright is consistently described as explicitly enabled experimental
  fallback, never the default transport;
- HTTP remains first in every monitoring cycle;
- confirmed discovery remains bounded by `LANDING -> DAYS -> TIMES -> STOP`;
- historical documents retain their observations and use dated supersession
  notes where current capability differs;
- v0.3.0 is dated 2026-08-02 and `[Unreleased]` is reopened separately.

## Final Decision

Technical Readiness, Governance Readiness, Traceability Readiness,
Operational Validation, Repository Hygiene, and Documentation Consistency all
pass. The repository is ready for maintainer review, staging, and the v0.3.0
release commit.

